import React from "react";

// the same as the header, but this is a footer! a component to be imported in other files such as App.js
const Footer = () => {
  return <h3> My Footer in another component </h3>;
};

export default Footer;
